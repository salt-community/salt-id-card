export type User = {
  fullName: string | null;
  image: string;
  email: string | undefined;
};

export type IdCard = {
  name: string;
  course: string;
  endDate: string;
  email: string | undefined;
  status: string;
};
