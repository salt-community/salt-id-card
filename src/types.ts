export type User = {
  name: string;
  course: string;
  endDate: string;
  email: string | undefined;
  status: string;
  image: string;
};

export type IdCard = {
  name: string;
  course: string;
  endDate: string;
  email: string | undefined;
  status: string;
};
