export type User ={
  fullName: string | null,
  image: string,
  email: string | undefined
  firstName: string | null
}

export type IdCard = {
  name: string;
  course: string;
  endDate: string;
  email: string;
  status: string;
};
